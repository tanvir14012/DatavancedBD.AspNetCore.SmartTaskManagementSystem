// TODO(SAAS-07): Compose queue consumer with a fresh tenant scope per job and bounded concurrency.
// TODO(SAAS-07): Test duplicate delivery, shutdown cancellation, retries, and stale placement rejection.
Console.Error.WriteLine("TODO(SAAS-07): SaaS worker is not implemented. No jobs consumed.");
return 1;
