trigger: none
pr: none
stages:
  - stage: GuardedCleanup
    jobs:
      - job: RequireExplicitApproval
        pool: { vmImage: ubuntu-latest }
        steps:
          - script: echo "Cleanup is intentionally a separately approved infrastructure operation; tenant provisioning is not part of application release."
            displayName: Guarded cleanup contract
