# TODO(SAAS-08): Replace with tested AKS release stages and workload identity.
# TODO(SAAS-06): Keep tenant provisioning separate from application releases.
trigger: none
pr: none
pool:
  vmImage: ubuntu-latest
steps:
  - checkout: none
  - script: echo "SaaS pipeline placeholder - no build, deployment or cleanup performed."
    displayName: Placeholder only
