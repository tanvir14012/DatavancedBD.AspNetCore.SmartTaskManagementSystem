name: SaaS placeholder
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      # TODO(SAAS-08): Build/test immutable artifacts, run Admin migration Job, deploy AKS and verify.
      # TODO(SAAS-06): Tenant provisioning must be a separate workflow; no cleanup is implemented.
      - name: Report placeholder
        run: echo "SaaS pipeline placeholder - no build, deployment or cleanup performed."
